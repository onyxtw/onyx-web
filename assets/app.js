const chatbotReplies={litu:"Litu / Lidao Base 是 ONYX 的核心實體節點，也是 G.E.M.S. 與 T.C.P. 的現地驗證層。",gems:"G.E.M.S. 是將運算熱負載與地熱 / 水熱結構耦合的 compute-material symbiosis framework。",tcp:"T.C.P. 定義在高限制 terrain 條件下，節點如何維持能源流、資料流與自主生存能力。",default:"目前這是可直接部署的前端版。正式版可把這個視窗接到 Cloudflare Workers + AI 模型 API。"};function initMobileMenu(){const btn=document.getElementById('menuBtn');const nav=document.getElementById('siteNav');if(btn&&nav)btn.addEventListener('click',()=>nav.classList.toggle('open'));}function initChat(){const trigger=document.getElementById('chatTrigger');const win=document.getElementById('chatWindow');const close=document.getElementById('chatClose');const form=document.getElementById('chatForm');const input=document.getElementById('chatInput');const box=document.getElementById('chatMessages');if(!trigger||!win||!form||!input||!box)return;const addMsg=(text,who='ai')=>{const div=document.createElement('div');div.className='msg '+who;div.textContent=text;box.appendChild(div);box.scrollTop=box.scrollHeight;};trigger.addEventListener('click',()=>{win.style.display=win.style.display==='flex'?'none':'flex';if(box.children.length===0){addMsg('我是 ONYX AI 視窗原型。你可以先詢問 Litu、G.E.M.S.、T.C.P. 或部署架構。');}});if(close)close.addEventListener('click',()=>win.style.display='none');form.addEventListener('submit',(e)=>{e.preventDefault();const q=input.value.trim();if(!q)return;addMsg(q,'user');input.value='';const key=q.toLowerCase();let reply=chatbotReplies.default;if(key.includes('litu')||key.includes('利稻')||key.includes('lidao'))reply=chatbotReplies.litu;else if(key.includes('gems'))reply=chatbotReplies.gems;else if(key.includes('tcp'))reply=chatbotReplies.tcp;setTimeout(()=>addMsg(reply,'ai'),300);});}async function loadFeed(){try{const res=await fetch('content/site-data.json');if(!res.ok)return;const data=await res.json();const research=document.getElementById('researchFeed');const updates=document.getElementById('updatesFeed');if(research){research.innerHTML=data.research.map(item=>'<article class="post"><div class="post-type">'+item.type+'</div><div class="post-title">'+item.title+'</div><div class="post-summary">'+item.summary+'</div></article>').join('');}if(updates){updates.innerHTML=data.updates.map(item=>'<article class="post"><div class="post-type">'+item.type+'</div><div class="post-title">'+item.title+'</div><div class="post-summary">'+item.summary+'</div></article>').join('');}}catch(e){console.error(e);}}document.addEventListener('DOMContentLoaded',()=>{initMobileMenu();initChat();loadFeed();});